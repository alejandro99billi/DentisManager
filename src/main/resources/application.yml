spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/mydatabase
    username: root
    password: mypassword
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
